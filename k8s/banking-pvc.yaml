apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: banking-ledger-pvc
  labels:
    app: banking-ledger
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
